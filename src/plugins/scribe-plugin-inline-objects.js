define('scribe-plugin-inline-objects',[],function () {

  /**
   * Adds support for inserting and managing inline objects, like embeds, videos and images.
   */


  return function (config) {
    return function (scribe) {
        console.log("I AM A PLUGIN. SEAN CONNERY.");




        console.log("config", config);

        // define inline objects

        /*
        scribe.transactionManager.run(function () {
          // Do some fancy DOM manipulation
        });
        */

    }
  }
});